import { Trispace } from "next/font/google";
export const trispace = Trispace({ subsets: ["latin"],weight: "200" });

import { Orbitron } from "next/font/google";
export const orbitron = Orbitron({ subsets: ["latin"], weight: "900" });